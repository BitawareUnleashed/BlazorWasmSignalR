@page "/"
@inject HttpClient Http
@using Microsoft.AspNetCore.SignalR.Client
@inject NavigationManager NavigationManager 
@implements IAsyncDisposable


<h1>Hello, world!</h1>
<p>Time of the day: <strong>@Watch</strong></p>
<div>
    @foreach (var item in messages)
    {
        <p>@item</p>
    }
</div>